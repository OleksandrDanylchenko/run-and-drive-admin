import { FC } from 'react';

import Container from '@mui/material/Container';
import Stack from '@mui/material/Stack';

const Dashboard: FC = () => {
  return (
    <Container maxWidth="sm">
      <Stack spacing={3}>helo!</Stack>
    </Container>
  );
};

export default Dashboard;
